Abstract Contract StringUtils() {

    fn calcMarketId(params: MarketParams) -> ByteVec {
        return keccak256!(params.loanToken ++ params.collateralToken ++ params.interestRateModel ++ params.oracle ++ toByteVec!(params.loanToValue))
    }
}

